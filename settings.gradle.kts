pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "GAI4-TDD"

plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}